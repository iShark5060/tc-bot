name: TC-Bot CI/CD

permissions:
  contents: read
  pull-requests: read

env:
  NODE_VERSION: 26

on:
  push:
    branches: [main]
    paths:
      - 'src/**'
      - 'package.json'
      - 'pnpm-lock.yaml'
      - 'pnpm-workspace.yaml'
      - 'ecosystem.config.cjs'
      - 'tsconfig*.json'
      - 'run-quality-checks.mjs'
      - '.env.production'
      - '.github/workflows/ci.yml'
      - '.releaserc.json'
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  deployment-check:
    if: ${{ !contains(github.event.head_commit.message, 'chore(release)') && !contains(github.event.head_commit.message, 'build(release)') }}
    runs-on: blacksmith-2vcpu-ubuntu-2404
    timeout-minutes: 10

    outputs:
      deploy: ${{ steps.decide.outputs.deploy }}

    steps:
      - name: Checkout full history
        uses: useblacksmith/checkout@v1
        with:
          fetch-depth: 0

      - name: Check if changes require deployment
        id: decide
        shell: bash
        env:
          BEFORE: ${{ github.event.before }}
          AFTER: ${{ github.sha }}
        run: |
          set -euo pipefail

          if [ -z "$BEFORE" ] || ! git cat-file -e "$BEFORE"^{commit} 2>/dev/null; then
            echo "deploy=true" >> "$GITHUB_OUTPUT"
            echo "Reason: Missing or invalid before commit"
            exit 0
          fi

          changed_files=$(git diff --name-only "$BEFORE" "$AFTER")
          echo "Changed files:"
          echo "$changed_files"

          if [ -z "$changed_files" ]; then
            echo "deploy=false" >> "$GITHUB_OUTPUT"
            echo "Reason: No files changed"
            exit 0
          fi

          others=$(echo "$changed_files" | grep -vE '(^|/)package\.json$|(^|/)pnpm-lock\.yaml$' || true)

          if [ -n "$others" ]; then
            echo "Other files changed:"
            echo "$others"
            echo "deploy=true" >> "$GITHUB_OUTPUT"
            echo "Reason: Non-package files changed"
            exit 0
          fi

          tmp_old=$(mktemp)
          tmp_new=$(mktemp)

          git show "$BEFORE:package.json" 2>/dev/null > "$tmp_old" || echo '{}' > "$tmp_old"
          git show "$AFTER:package.json" 2>/dev/null > "$tmp_new" || echo '{}' > "$tmp_new"

          if ! jq -e --slurp \
            '.[0] as $new | .[1] as $old |
             ($new | del(.devDependencies)) == ($old | del(.devDependencies))' \
            "$tmp_new" "$tmp_old" >/dev/null; then
            echo "deploy=true" >> "$GITHUB_OUTPUT"
            echo "Reason: Production dependencies changed"
            exit 0
          fi

          echo "deploy=false" >> "$GITHUB_OUTPUT"
          echo "Reason: Only devDependencies or lockfile changed"

  version:
    runs-on: blacksmith-2vcpu-ubuntu-2404
    timeout-minutes: 10

    needs: [deployment-check]
    if: ${{ needs.deployment-check.outputs.deploy == 'true' && !contains(github.event.head_commit.message, 'chore(release)') && !contains(github.event.head_commit.message, 'build(release)') }}

    permissions:
      contents: write
    outputs:
      target_sha: ${{ steps.target.outputs.sha }}

    steps:
      - name: Checkout full history
        uses: useblacksmith/checkout@v1
        with:
          fetch-depth: 0
          token: ${{ secrets.CI_PAT }}

      - name: Setup pnpm
        uses: pnpm/action-setup@v6

      - name: Setup Node.js
        uses: actions/setup-node@v7
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: pnpm

      - name: Install dependencies (fresh lockfile)
        uses: ./.github/actions/pnpm-install-fresh

      - name: Bump version
        run: pnpm exec semantic-release
        env:
          GITHUB_TOKEN: ${{ secrets.CI_PAT }}
          GITHUB_REPOSITORY: ${{ github.repository }}
          MERGE_COMMIT_MESSAGE: ${{ github.event.head_commit.message }}

      - name: Resolve deployment target SHA
        id: target
        shell: bash
        run: |
          git fetch origin main
          echo "sha=$(git rev-parse origin/main)" >> "$GITHUB_OUTPUT"

  validate:
    runs-on: blacksmith-2vcpu-ubuntu-2404
    timeout-minutes: 10

    needs: [deployment-check, version]
    if: needs.deployment-check.outputs.deploy == 'true'

    env:
      DOTENV_PRIVATE_KEY_PRODUCTION: ${{ secrets.DOTENV_PRIVATE_KEY_PRODUCTION }}

    steps:
      - name: Checkout release target
        uses: useblacksmith/checkout@v1
        with:
          ref: ${{ needs.version.outputs.target_sha }}

      - name: Setup pnpm
        uses: pnpm/action-setup@v6

      - name: Setup Node.js
        uses: actions/setup-node@v7
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'pnpm'

      - name: Install dependencies (fresh lockfile)
        uses: ./.github/actions/pnpm-install-fresh

      - name: Run quality checks
        run: |
          if [ -z "$DOTENV_PRIVATE_KEY_PRODUCTION" ]; then
            echo "ERROR: DOTENV_PRIVATE_KEY_PRODUCTION secret is missing or empty."
            exit 1
          fi
          pnpm exec dotenvx run --strict -f .env.production -- pnpm run validate

  build-and-deploy:
    runs-on: blacksmith-2vcpu-ubuntu-2404
    timeout-minutes: 10

    needs: [deployment-check, version, validate]
    if: needs.deployment-check.outputs.deploy == 'true'

    steps:
      - name: Checkout release target
        uses: useblacksmith/checkout@v1
        with:
          ref: ${{ needs.version.outputs.target_sha }}

      - name: Setup pnpm
        uses: pnpm/action-setup@v6

      - name: Setup Node.js
        uses: actions/setup-node@v7
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'pnpm'

      - name: Install dependencies (fresh lockfile)
        uses: ./.github/actions/pnpm-install-fresh

      - name: Build native modules
        run: pnpm rebuild better-sqlite3 esbuild

      - name: Create client_secret.json from secret
        env:
          GOOGLE_CLIENT_SECRET_BASE64: ${{ secrets.GOOGLE_CLIENT_SECRET_BASE64 }}
        run: |
          if [ -z "$GOOGLE_CLIENT_SECRET_BASE64" ]; then
            echo "ERROR: GOOGLE_CLIENT_SECRET_BASE64 secret is missing or empty."
            exit 1
          fi
          printf '%s' "$GOOGLE_CLIENT_SECRET_BASE64" | base64 -d > client_secret.json

      - name: Build TypeScript
        env:
          DOTENV_PRIVATE_KEY_PRODUCTION: ${{ secrets.DOTENV_PRIVATE_KEY_PRODUCTION }}
        run: |
          if [ -z "$DOTENV_PRIVATE_KEY_PRODUCTION" ]; then
            echo "ERROR: DOTENV_PRIVATE_KEY_PRODUCTION secret is missing or empty."
            exit 1
          fi
          pnpm exec dotenvx run --strict -f .env.production -- pnpm run build

      - name: Deploy slash commands
        env:
          DOTENV_PRIVATE_KEY_PRODUCTION: ${{ secrets.DOTENV_PRIVATE_KEY_PRODUCTION }}
          TOKEN: ${{ secrets.TOKEN }}
          CLIENT_ID: ${{ secrets.CLIENT_ID }}
        run: |
          if [ -z "$TOKEN" ] || [ -z "$CLIENT_ID" ]; then
            echo "Skipping command deploy: TOKEN or CLIENT_ID secret missing."
            exit 0
          fi
          pnpm exec dotenvx run --strict -f .env.production -- pnpm run deploy

      - name: List built files (debug)
        run: ls -la dist/

      - name: Setup SSH
        env:
          SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
          SERVER_KNOWN_HOST: ${{ secrets.SERVER_KNOWN_HOST }}
          SERVER_HOST: ${{ secrets.SERVER_HOST }}
          SERVER_USER: ${{ secrets.SERVER_USER }}
          SERVER_PATH: ${{ secrets.SERVER_PATH }}
          SERVER_SSH_PORT: ${{ secrets.SERVER_SSH_PORT }}
        run: |
          require_non_empty() {
            name="$1"
            value="$2"
            if [ -z "$value" ]; then
              echo "ERROR: $name is missing or empty."
              exit 1
            fi
          }

          require_non_empty "SSH_PRIVATE_KEY" "$SSH_PRIVATE_KEY"
          require_non_empty "SERVER_KNOWN_HOST" "$SERVER_KNOWN_HOST"
          require_non_empty "SERVER_HOST" "$SERVER_HOST"
          require_non_empty "SERVER_USER" "$SERVER_USER"
          require_non_empty "SERVER_PATH" "$SERVER_PATH"

          case "$SERVER_PATH" in
            ""|"/"|"."|".."|"~"|"~/"|"/home"|"/root")
              echo "ERROR: SERVER_PATH has unsafe value: '$SERVER_PATH'"
              exit 1
              ;;
          esac
          case "$SERVER_PATH" in
            "~"*|*".."*|*"/./"*|*"/../"*|*"//"*)
              echo "ERROR: SERVER_PATH contains unsafe path segments: '$SERVER_PATH'"
              exit 1
              ;;
          esac

          mkdir -p ~/.ssh
          echo "$SSH_PRIVATE_KEY" > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
          if [ -z "$SERVER_SSH_PORT" ]; then
            echo "ERROR: SERVER_SSH_PORT secret is missing or empty."
            exit 1
          fi
          echo "$SERVER_KNOWN_HOST" >> ~/.ssh/known_hosts

      - name: Prepare deployment
        env:
          DOTENV_PRIVATE_KEY_PRODUCTION: ${{ secrets.DOTENV_PRIVATE_KEY_PRODUCTION }}
        run: |
          mkdir -p /tmp/deploy
          cp -r dist/ /tmp/deploy/
          cp package*.json /tmp/deploy/
          cp pnpm-workspace.yaml /tmp/deploy/
          cp -r node_modules/ /tmp/deploy/
          cp ecosystem.config.cjs /tmp/deploy/
          cp client_secret.json /tmp/deploy/
          cp .env.production /tmp/deploy/
          printf 'DOTENV_PRIVATE_KEY_PRODUCTION=%s\n' "$DOTENV_PRIVATE_KEY_PRODUCTION" > /tmp/deploy/.env.keys
          chmod 600 /tmp/deploy/.env.keys
          test -s /tmp/deploy/.env.keys || { echo "ERROR: Failed to create .env.keys in deployment bundle."; exit 1; }

      - name: Deploy to server
        env:
          SERVER_SSH_PORT: ${{ secrets.SERVER_SSH_PORT }}
          SERVER_USER: ${{ secrets.SERVER_USER }}
          SERVER_HOST: ${{ secrets.SERVER_HOST }}
          SERVER_PATH: ${{ secrets.SERVER_PATH }}
        run: |
          rsync -av --no-times --delete \
            --exclude='.env' \
            --exclude='.env.keys' \
            --exclude='logs/' \
            --exclude='data/' \
            --chmod=D2770,F640 \
            --omit-dir-times \
            -e "ssh -p $SERVER_SSH_PORT -i ~/.ssh/deploy_key -o StrictHostKeyChecking=yes -o UserKnownHostsFile=~/.ssh/known_hosts" \
            /tmp/deploy/ \
            "$SERVER_USER@$SERVER_HOST:$SERVER_PATH/"

          rsync -av --no-times \
            --chmod=F600 \
            --omit-dir-times \
            -e "ssh -p $SERVER_SSH_PORT -i ~/.ssh/deploy_key -o StrictHostKeyChecking=yes -o UserKnownHostsFile=~/.ssh/known_hosts" \
            /tmp/deploy/.env.keys \
            "$SERVER_USER@$SERVER_HOST:$SERVER_PATH/.env.keys"

      - name: Update permissions
        env:
          SERVER_SSH_PORT: ${{ secrets.SERVER_SSH_PORT }}
          SERVER_USER: ${{ secrets.SERVER_USER }}
          SERVER_HOST: ${{ secrets.SERVER_HOST }}
          SERVER_PATH: ${{ secrets.SERVER_PATH }}
        run: |
          ssh -p "$SERVER_SSH_PORT" -i ~/.ssh/deploy_key \
            -o StrictHostKeyChecking=yes -o UserKnownHostsFile=~/.ssh/known_hosts \
            "$SERVER_USER@$SERVER_HOST" \
            "/usr/local/bin/pm2-chown-tree $SERVER_PATH"

      - name: Restart PM2
        env:
          SERVER_SSH_PORT: ${{ secrets.SERVER_SSH_PORT }}
          SERVER_USER: ${{ secrets.SERVER_USER }}
          SERVER_HOST: ${{ secrets.SERVER_HOST }}
        run: |
          ssh -p "$SERVER_SSH_PORT" -i ~/.ssh/deploy_key \
            -o StrictHostKeyChecking=yes \
            -o UserKnownHostsFile=~/.ssh/known_hosts \
            "$SERVER_USER@$SERVER_HOST" \
            "/usr/local/bin/pm2-restart-safe TC-Bot"

      - name: Cleanup SSH key
        if: always()
        run: rm -f ~/.ssh/deploy_key

  discord-status:
    runs-on: blacksmith-2vcpu-ubuntu-2404
    timeout-minutes: 10
    needs: [deployment-check, validate, build-and-deploy, version]
    if: ${{ always() && github.ref == 'refs/heads/main' }}

    steps:
      - name: Send Discord status
        uses: iShark5060/actions-discord-status@v1
        with:
          webhook: ${{ secrets.DISCORD_WEBHOOK }}
          job_results: ${{ join(needs.*.result, ',') }}
          mention_on: failure
          content: '<@${{ secrets.DISCORD_USERID }}>'
          title: ${{ github.workflow}}
          description: ${{ contains(needs.*.result, 'failure') && 'CI failed. Please review logs before redeploying.' || contains(needs.*.result, 'cancelled') && 'CI was cancelled before completion.' || needs.deployment-check.outputs.deploy == 'false' && 'CI was skipped because deployment-check determined no deployable changes.' || contains(needs.*.result, 'skipped') && 'CI was skipped due to upstream conditions.' || 'CI succeeded. New build is deployed and available now.' }}
